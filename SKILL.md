---
name: "project-arch-reverse"
version: "3.0"
description: "逆向分析项目架构。自动检测项目类型（Android/iOS/Java后端/Vue/React/Flutter/Node.js/HarmonyOS Next等），分析模块依赖关系，生成含模块依赖图、数据库设计、页面导航的架构文档。流程图标注方法名+代码文件:行号。支持增量更新、人工更正和自检验证。Trigger: 中文"分析项目结构" "生成架构文档" "项目架构" "逆向分析" "生成项目文档" "模块架构文档" "架构逆向" "项目分析" "自检架构文档" "检查架构完整性" | English "analyze project structure" "generate architecture docs" "reverse engineer project" "project architecture analysis" "module dependency analysis""
---

# 项目架构逆向工程

> **一句话**：检测项目 → 扫描模块 → 生成全量架构文档（含代码位置标注）→ 自检验证 → 交付

---

## 场景速查

| 你想做什么 | 说这句话 | 自动策略 | 预计耗时 |
|-----------|---------|---------|---------|
| 首次全量分析 | "帮我分析这个项目" | [A] 全量生成 + [L-A] 全量日志 | 3-10分钟 |
| 更新已有文档 | "项目代码有变化，更新文档" | [C] 增量更新 + [L-C] 同步更新日志 | 1-5分钟 |
| 补充新模块 | "新增了X模块，补上文档" | [B] 补充生成 + [L-B] 同步补充日志 | 1-2分钟 |
| 检查完整性 | "帮我检查文档完整性" | [CP6] 自检验证 | 1-3分钟 |
| 只看依赖 | "帮我分析模块依赖" | 跳过文档，只输出依赖矩阵+循环检测 | <1分钟 |

---

## 核心流程

> **必须严格按顺序**：CP0 → CP1 → CP2 → CP3 → CP4 → CP5 → CP6 → CP7

| CP | 名称 | 输入 | 输出 | 核心动作 |
|----|------|------|------|---------|
| CP0 | 增量检测 | 工作目录 | 执行策略 + 模块范围 | 扫描已有文档 → 判定核心模块 → 选策略 → **等用户确认** |
| CP1 | 类型确认 | CP0 输出 | 项目类型 + 模块范围 | 文件签名检测 → 加载配置 → 用户确认 |
| CP2 | codegraph | CP1 输出 | 分析模式 | 检测可用性 → 可用则走分析管道 / 不可用降级手动 |
| CP3 | 扫描 | CP1 输出 | 依赖矩阵 + 拓扑排序 + 图表 | 目录扫描 → 依赖提取 → 循环检测 → 拓扑排序 → 入口扫描 → Mermaid |
| CP4 | 生成 | CP3 输出 | 完整架构文档 | **模块优先**：先模块文档 → 再聚合根文档 → 日志文档同步 |
| CP5 | 结果确认 | CP4 输出 | 用户确认 | 展示文档 → 收集反馈 → 满足/修改/人工更正 |
| CP6 | 自检验证 | CP5 确认 | 全部通过或阻塞 | 6步检查 + 循环修复 + **硬门禁** |
| CP7 | 人工更正 | CP5反馈 / CP6阻塞 | 更正后文档 | 逐项确认 → 回归CP6 |

**核心原则**：
- 🔄 **模块优先**：按拓扑排序分层生成全部模块文档 → 再聚合根文档
- 📊 **聚合展示**：根文档第12章全量汇总所有核心功能（含跨模块分析）
- 🔗 **交叉引用**：根文档内容聚合自模块文档，标注来源
- 🧠 **codegraph 优先**：CP2 自动检测 `@optave/codegraph`，可用时替代手动扫描

---

### CP0：增量检测（必须，不可跳过）

1. 扫描已有文档：
   - `docs/architecture/*.项目架构文档.md` → 根文档
   - `docs/architecture/modules/**/*模块架构.md` → 模块文档
   - `docs/logs/**/*_核心日志.md` → 日志文档
2. 扫描任务清单状态 ★：
   - `docs/architecture/.task-checklist.md` 不存在 → 正常流程
   - 存在且全部 `[x]`/`[!]` → 任务已完成
   - 存在且有 `[ ]`/`[>]` → **未完成** → 推荐"恢复执行"
   - 用户可选：恢复执行 / 放弃重新开始
3. 扫描项目构建文件，识别所有模块
4. 判定核心模块（满足**任一**即为核心）：

   | 维度 | 条件 | 示例 |
   |------|------|------|
   | 用户可达 | 含UI页面/界面 | app、feature:* |
   | 后台运行 | Service/Worker/定时/推送/同步 | core:sync |
   | 复杂度高 | >20文件 或 >5000行 | 核心业务层 |
   | 用户指定 | 用户明确告知 | 任意模块 |

   > **非核心**：构建脚本、Gradle插件、纯工具库（无UI无后台且复杂度低）、测试模块

5. 选择策略：

   | 策略 | 触发条件 | 动作 | 日志策略 | 日志动作 |
   |------|---------|------|---------|---------|
   | **A 全量** | docs/architecture/ 不存在 | 创建全部文档 | L-A | 为所有核心模块生成 |
   | **B 补充** | 根文档存在，模块部分缺失 | 生成缺失模块 | L-B | 补充缺失模块 |
   | **C 增量** | 根文档+模块文档均存在 | 对比时间戳更新 | L-C | 对比代码时间戳更新 |
   | **D 重建** | 根文档不存在，模块存在 | 基于模块重建根文档 | — | — |

   > ⚠️ **架构与日志必须同步**：选择架构策略 A/B/C/D 时，同步执行对应日志策略。**不存在"只更新架构不更新日志"**。

6. ⚠️ **输出检测报告并等待用户确认**（不可跳过）：
   展示：模块清单 / 已有文档 / 推荐策略 / 核心模块列表
   用户可覆盖：模块范围 / 核心标记 / 策略选择 / 项目类型 / 跳过codegraph
   > 用户指定 > 自动检测，用户确认后才进入 CP1

---

### CP1：项目类型确认（必须）

1. 文件签名检测（详见 `references/project-types-config.md`）
2. 确认范围：全部模块 / 指定模块 → 用户确认

**混合项目处理**：检测到多种类型签名时 → 识别主类型（最多文件匹配）→ 列出子类型 → 用户确认划分 → 分别使用对应模板

**类型误判回退**：用户指定的类型与自动检测不一致 → **以用户指定为准**，使用指定类型的配置和模板

---

### CP2：codegraph 可用性检测（推荐，可跳过）

> 优先使用 `@optave/codegraph` (v3.11.1, 34 语言支持)。不可用时自动降级为手动扫描。

```
检测流程:
  npm --version
    ├── npm 可用 → 继续
    │   npm ls -g @optave/codegraph
    │     ├── 已安装 → 检查 .codegraph/graph.db
    │     │   ├── 存在 → ✅ codegraph 分析模式
    │     │   └── 不存在 → codegraph build → 成功则分析模式
    │     └── 未安装 → 询问用户"是否全局安装 @optave/codegraph？"
    │                   ├── 同意 → npm install -g @optave/codegraph → codegraph build
    │                   │   ├── 成功 → ✅ codegraph 分析模式
    │                   │   └── 失败 → ⚠️ 降级: 手动扫描模式
    │                   └── 拒绝 → ⚠️ 降级: 手动扫描模式
    └── npm 不可用 → 询问用户"未检测到 npm 环境，是否安装 Node.js？"
          ├── 同意 → 安装 → 继续检测
          └── 拒绝 → ⚠️ 降级: 手动扫描模式
```

**🔬 codegraph 分析管道** ★（codegraph 可用时强制走此管道，手动扫描仅兜底不可覆盖章节）：

**Step A — 查询节点数据**：
1. 查询 role=entry 节点 → 核心功能入口清单
2. 查询 role=core 节点 → 核心能力候选 + 关键函数（按模块分组统计）
3. 查询 top N by 认知复杂度 + 被调用次数 → 关键类/方法
4. 查询 role=dead 节点 → 死函数清单（按模块分组）
5. 查询 role=utility/adapter 节点 → 工具/适配层占比
6. 查询模块间调用边 → 依赖矩阵
7. 查询跨模块调用链 + SCC 循环依赖 → 跨模块分析 + 重构建议
8. 执行 codegraph check（默认规则）→ manifesto 违规清单
9. 对每个核心能力 entry→core→leaf 调用链，逐步骤 grep 日志语句 → 日志存在: 提取级别+内容+文件:行号 / 缺失: 标注 [!] → 分析 try-catch 异常传播

**Step B — 为每个 core 节点生成核心能力档案**（必须，不可省略）：
```markdown
### 核心能力: <函数名>
**用途**: <基于函数名语义+代码推断，标注"[需人工确认]">
**复杂度**: 认知 X | 圈 Y | 代码位置: <文件:行号>

**调用者**: | 调用方 | 触发类型 | 代码位置 |
**被调用者**: | 函数 | 所在模块 | 代码位置 |
**当前场景**: <从调用者推导的触发场景列表>
**时序图** [≥6步, ≥4参与者]: [mermaid sequenceDiagram]
**业务流程图** [分支逻辑]: [mermaid flowchart]
```

**Step C — 章节映射**（codegraph 产出 → 文档章节）：

| 文档 | codegraph 覆盖章节 | 手动覆盖章节 |
|------|-------------------|-------------|
| 根文档 | §6依赖图, §7依赖分析, §8模块介绍, §12.3核心功能, §12.4跨模块, §13 ER图, §14 类图, §22重构建议★, §23死代码★ | §10主页架构, §11页面跳转, §15-19 ADR~风险 |
| 模块文档 | §1概述, §4业务流程, §5数据流, §6核心功能, §11关键类, §13重构建议★, §14死代码★ | §2 UI组件, §9页面路由, §10导航示例, §12测试策略 |
| 核心日志 | §3执行路径日志追踪, §4错误传播与异常, §5日志覆盖评估, §8故障排查决策树★ | — |

> ★ 标注章节仅在 codegraph 模式下生成（手动模式保持 21+12+5 章）

**降级回退规则**：
- npm 不可用且用户拒绝 → 降级手动
- codegraph 安装/构建失败 → 降级手动
- 用户 CP0 指定跳过 codegraph → 降级手动
- 降级不报错，报告标注"codegraph 不可用，使用手动分析"

安装速查：`npm install -g @optave/codegraph` | 构建：`codegraph build` | 文档：https://github.com/optave/ops-codegraph-tool

---

### CP3：扫描阶段

| 步骤 | 动作 | 说明 |
|------|------|------|
| 1 | 目录扫描 | 深度≤5 |
| 2 | 构建解析 | 提取依赖 → 输出依赖矩阵表格 |
| 3 | 循环检测 | 识别循环依赖 |
| 4 | 拓扑排序 ★ | Kahn 算法按依赖深度分层：Layer 0 → Layer 1 → ...；循环依赖模块合并同层；同层可并行生成 |
| 5 | 入口点扫描 ★ | 按项目类型加载配置 grep 搜索 → 输出入口清单（方法名 + 文件:行号 + 触发类型：UI触发/系统触发/外部事件） |
| 6 | 导航分析 | nav*.xml / router / App.tsx 等 |
| 7 | 生成图表 | Mermaid — 每节点标注方法名+文件:行号（三步法见附录） |
| 8 | 加载模板 | 按类型: `android-template.md` `ios-template.md` `java-backend-template.md` `vue-template.md` `react-template.md` `flutter-template.md` `nodejs-template.md` `harmonyos-template.md`（详见 `references/`） |

**扫描命令速查**：
```bash
# Android  find . -maxdepth 3 -name "build.gradle*" | head -20
# iOS      find . -maxdepth 3 \( -name "*.xcodeproj" -o -name "Podfile" \)
# Vue      find . -maxdepth 2 \( -name "package.json" -o -name "src" \) | head -5
# Java     find . -maxdepth 2 \( -name "pom.xml" -o -name "build.gradle*" \)
# Flutter  find . -maxdepth 3 -name "pubspec.yaml"
# Harmony  find . -maxdepth 3 \( -name "hvigorfile.ts" -o -name "oh-package.json5" \)
```

**依赖提取命令速查**（按项目类型）：
```bash
# Android: grep -E "(implementation|api|compileOnly)" build.gradle*
# iOS:     grep -E "s\.dependency|pod '" Podfile
# Vue/React: grep -E '"dependencies"' package.json -A 20
# Java:    grep -E "<dependency>" pom.xml
# Flutter: grep -E "^\s+\w+:" pubspec.yaml
# Node.js: jq '.dependencies' package.json
```
**依赖矩阵格式**：行=调用方模块, 列=被调用方模块, 值=依赖类型(强/弱/可选)。详见 `references/quick-ref.md` | `references/doc-format.md`

---

### CP4：生成阶段

> ⚠️ **模块优先 + 强制完整**：先生成全部模块文档（按拓扑排序），再聚合根文档。**所有模块 + 所有核心模块日志必须生成，不可省略！**

**按策略执行**：

| 策略 | 执行步骤 |
|------|---------|
| **A** | 创建目录 → 生成任务清单 `.task-checklist.md` → 按拓扑序逐章生成所有模块文档(12章) → 各模块日志(5章) → **基于模块文档聚合生成根文档(21/23章)** |
| **B** | 读现有文档 → 识别缺失 → 生成缺失任务清单 → 逐章补充 → 补充缺失日志 → **重新聚合根文档** |
| **C** | 对比代码 vs 文档时间戳 → 生成变更任务清单 → 逐章更新 → 同步更新日志 → 新模块生成文档+日志 → **重新聚合根文档** |
| **D** | 生成聚合任务清单 → 基于已有模块文档聚合根文档 → 补充缺失模块文档 → 补充缺失日志 |

**📋 任务清单执行机制** ★：
- 按拓扑排序生成章节级 `.task-checklist.md`（每模块 12 章 + 日志 5 章 + 根文档聚合）
- 状态转换：`[ ]` 未开始 → `[>]` 进行中 → `[x]` 完成；失败 `[>]` → `[!]` 跳过
- 中断恢复：CP0 检测到未完成任务清单 → 从第一个 `[ ]` 继续
- 任务清单文件位于 `docs/architecture/.task-checklist.md`（格式见 `references/doc-format.md`）

**📋 根文档聚合执行清单**（22 步，按序执行）：

| # | 章节 | 数据来源 | 操作 |
|---|------|---------|------|
| 1 | §1 修订历史 | 当前时间戳 | 新建表格，v1.0 + 日期 + "自动生成" |
| 2 | §2 架构概览 | 模块清单 + 拓扑排序 | 概述：模块数、分层关系、架构模式 |
| 3 | §3 项目简介 | 目录结构 + 构建文件 | 项目名、推断用途、技术栈摘要 |
| 4 | §4 术语 | 各模块 §1 术语 | 聚合去重，生成术语表 |
| 5 | §5 技术栈 | 构建文件 | 聚合各模块引用的库/框架 |
| 6 | §6 依赖图 | 依赖矩阵/各模块 §3 | codegraph Mermaid 导出 或 手动绘制 |
| 7 | §7 依赖分析 | §6 + SCC | 文本描述 + 循环依赖报告 |
| 8 | §8 模块介绍 | 各模块 §1 概述 | 按拓扑序聚合，1-2 句话/模块 |
| 9 | §9 主要组件 | 各模块 §2 UI | 聚合核心组件清单 |
| 10 | §10 主页架构 | 导航分析 | 手动绘制页面结构 Mermaid |
| 11 | §11 页面跳转 | router 配置 | 手动分析路由表 |
| 12 | §12.1 整体概览 | 导航 + 各模块 §6 | 跨模块业务概览 Mermaid |
| 13 | §12.2 索引表 | 各模块 §6 | 核心功能索引 (F-001, F-002...) |
| 14 | §12.3 模块汇总 | 各模块 §6 档案 | 逐模块展示核心能力档案 |
| 15 | §12.4 跨模块 | 跨模块调用链 | 端到端时序图 ≥6步 |
| 16 | §13 ER图 | 各模块 §7 实体 | 聚合 → Mermaid erDiagram |
| 17 | §14 类图 | 各模块 §11 关键类 | 聚合 → classDiagram |
| 18 | §15-§19 ADR~风险 | 手动 | P3 可简，标注"待补充" |
| 19 | §20 模块索引 | 模块文档路径 | 自动生成链接列表 |
| 20 | §21 编译指南 | build-guide | 按项目类型加载编译模板 |
| 21 | §22 重构建议 | codegraph check | manifesto违规 + 复杂度热点 |
| 22 | §23 死代码 | codegraph dead | 死函数清单 + 死模块 + 统计 |

> 手动模式 / codegraph 不可用时跳过 §22-§23。

**文档结构**：
```
{项目根}/
└── docs/
    ├── architecture/
    │   ├── {项目名}项目架构文档.md        # 根文档 23章(codegraph) / 21章(手动)
    │   └── modules/{模块名}/
    │       └── {模块名}模块架构.md         # 模块文档 14章 / 12章(手动)
    └── logs/{模块名}/
        └── {模块名}_核心日志.md            # 日志文档 8章(codegraph) / 5章(手动)
```

**章节速查**（完整定义见 `references/chapters-ref.md`）：

| 文档 | 章数 | 必须标注代码位置的章节 |
|------|------|---------------------|
| 根文档 | 23/21 | §6依赖图、§10主页架构、§12业务流程、§13 ER图、§14 类图、§22重构、§23死代码 |
| 模块文档 | 14/12 | §4业务流程、§5数据流、§6核心功能、§11关键类、§13重构、§14死代码 |
| 核心日志 | 8/5 | §3执行路径日志追踪、§4错误传播、§5日志覆盖评估、§8故障排查决策树 |

> 💡 最小可行文档：至少完成 P0+P1+P2 章节（见 `references/chapters-ref.md` 优先级表）

**根文档第12章聚合模式** ★：全量聚合展示，一切内容来自模块文档：
- §12.1 整体业务概览图（跨模块全景）
- §12.2 核心功能索引表（功能ID/名称/模块/类型/节号）
- §12.3 模块级核心功能汇总（逐模块聚合，每功能独立时序图+步骤详解+代码）
- §12.4 跨模块综合功能分析（端到端时序图，多模块参与者）

**核心功能/核心能力判定**（核心模块中满足**任一**）：
- 用户直接交互（按钮/表单/导航）
- 后台自主运行（定时同步/推送/预加载）
- 功能复杂度高（复杂计算/多层调用链/自定义协议）
- 数据流转关键节点（数据转换/状态管理/缓存策略）
- **平台入口触发**（ContentProvider/@Scheduled/BroadcastReceiver 等系统级入口）

> 🔴 **强制：每个核心功能必须有独立时序图**，禁止合并多个功能为一个粗略时序图。

---

### CP5：结果确认（必须）

1. 展示文档（根文档 + 模块文档索引）
2. 收集反馈：
   - 满足需求 → 继续 CP6
   - 需要修改 → 执行修改 → 重新确认
   - 需要人工更正 → 进入 CP7

---

### CP6：自检验证（必须，不可跳过 — 硬门禁）

> 🔴 **硬门禁**：自检无循环次数上限，唯一终止条件是 **全部检查项通过**。不可带着问题进入下一阶段。

| 步骤 | 检查项 | 发现问题时的修复操作 |
|------|--------|-------------------|
| 1 | **模块覆盖与完整性**（构建文件识别数 vs 已生成数 + 每文档12章完整） | 缺失→立即生成；章节缺失→补充 |
| 2 | **模块间依赖一致性**（A模块§3声明依赖B → B存在且方向正确） | 不一致→以构建文件解析为准修正 |
| 3 | **根文档21章完整性** | 缺失→补充；空/占位符→重新聚合 |
| 4 | **根文档聚合正确性**（§6↔模块§3、§12↔模块§6、§13↔模块§7、§14↔模块§11） | 不一致→重新聚合生成 |
| 5 | **核心日志文档完整性 + 架构变更同步** | 缺失→提取日志；变更→同步更新 |
| 6 | **聚合图表交叉验证**（§12时序图↔模块§6时序图：步骤数、参与者、调用顺序一致） | 不一致→以模块文档为准修正 |

**判定与处理**：
```
全部通过 ✅ → 继续 CP7 或完成

有未通过项 ❌
  ├── 可自动修复 → 自动修复 → 重新自检（全项）
  │   同一问题第3次仍失败 → 标记阻塞 → 委托 CP7
  └── 不可自动修复 → 标记阻塞 → 委托 CP7 → 人工处理 → 回归 CP6
```

**防死循环**：
- 同一问题的同一检查项 3 次失败 → 标记阻塞，委托 CP7
- 全项自检累计 5 轮仍有阻塞 → 暂停请求用户决策：继续 / 降级 / 接受
- 用户明确接受某检查项 → 标记"用户接受"，后续跳过

**终止条件**：全部 6 项通过 / 用户主动确认接受残留并退出

> ⚠️ CP6 ↔ CP7 交互：阻塞项委托 CP7 处理 → CP7 完成 → 自动回归 CP6（从 Step 1 全项重新自检）

详见：`references/self-check-spec.md`

---

### CP7：人工更正（可选 / CP6 委托回归）

**两种触发方式**：
1. **独立触发**（用户主动）：CP5 确认后直接进入 → 展示文档 → 收集反馈 → 执行更正 → 重复直到满意
2. **委托触发**（CP6 阻塞）：CP6 输出阻塞项清单 → 用户逐项确认或修正 → 处理完成后 **自动回归 CP6**

---

## 异常处理

| 场景 | 处理 |
|------|------|
| 文件不可读 | 跳过 + 警告 |
| 构建解析失败 | 目录结构推断 |
| 模块过多 (>50) | 分批（≤20/批） |
| 表过多 (>100) | 只主要表（≥3引用） |
| 非标准结构 | 输出已扫描 + 询问 |
| 增量失败 | 备份 + 全量生成 |
| 导航缺失 | 跳过 + 警告 |
| 循环依赖 | 输出矩阵 + 建议 |
| 模块无源码 | 记录 + 报告 + 询问 |
| 模块文档生成失败 | 记录 + 继续下一个 + 最终报告 |
| 核心日志提取失败 | 记录 + 标记待处理 + 继续 |
| 日志语句不足 (<3) | 生成文档但标注"日志覆盖不完整" |
| 混合项目类型 | 识别主类型+子类型，用户确认划分，分别使用对应模板 |
| Monorepo多包 | 每个包独立识别类型+模块，生成子根文档+统一索引 |
| 用户覆盖类型 | 以用户指定为准，使用指定类型的配置和模板 |
| CP1类型误判 | 用户可在CP0/CP1纠正，确认后使用纠正后的类型 |

---

## 附录：关键规范速查

### Mermaid 标注三步法

① 节点文本用 `"方法名\n文件:行号"` 格式
② 时序图参与者标注模块名
③ 每个图表下方附 API 详解表（方法名/参数/返回值/文件:行号）

### 时序图详细度要求 ★

- **最少步骤**：≥6 个调用步骤（不含返回箭头），覆盖完整调用链路
- **分层完整**：必须覆盖表现层→业务层→数据层（UI→Service→Repository→DataSource），不能跳层
- **参与者数量**：≥4 个参与者，展示跨组件/跨层交互
- **返回消息**：每个请求必须有对应返回（`-->>`），关键返回标注转换逻辑
- **步骤详解表**：每个时序图下方附带步骤详解表 + 关键代码示例
- **禁止粗略**：不允许 `UI->>Repo: getData()` 跳过中间层的简化时序

### 代码位置标注规范

**所有图表**（依赖图、流程图、时序图、导航图、类图、ER图）中涉及代码的节点/参与者都必须标注 `方法名` + `文件路径:行号`，并附步骤 API 详解表。**所有关键代码步骤必须在文档中展示代码片段**（必需，非推荐）。

详见：`references/mermaid-spec.md` | `references/chapters-ref.md`

---

## 参考文档

| 文件 | 用途 |
|------|------|
| `references/quick-ref.md` | 快速参考（执行预估、策略决策树、命令） |
| `references/chapters-ref.md` | 章节速查（21+12+5章详情+优先级） |
| `references/mermaid-spec.md` | Mermaid图表规范（含标注三步法+示例） |
| `references/doc-format.md` | 文档格式规范（表格、代码块、依赖矩阵） |
| `references/self-check-spec.md` | 自检验证详细规格（含报告格式） |
| `references/project-types-config.md` | 类型检测 + 文件签名 + 特有分析 |
| `references/log-extraction-guide.md` | 各语言日志提取指南（含扫描命令） |
| `references/template.md` | 通用模板 |
| 各类型模板 | `references/android-template.md` `references/ios-template.md` `references/java-backend-template.md` `references/vue-template.md` `references/react-template.md` `references/flutter-template.md` `references/nodejs-template.md` `references/harmonyos-template.md` |
| 各编译指南 | `references/build-guide-android.md` `references/build-guide-ios.md` `references/build-guide-java-backend.md` `references/build-guide-vue.md` `references/build-guide-react.md` `references/build-guide-flutter.md` `references/build-guide-nodejs.md` `references/build-guide-harmonyos.md` |

---

## 资源使用原则

- ✅ 按需加载，不预加载所有资源
- ✅ 路径安全：所有文件操作经过安全校验
- ✅ 错误恢复：失败时自动降级到备选方案
- ✅ 用户确认：关键操作前必须获得确认
- ✅ **强制生成：所有模块文档 + 核心模块日志文档必须生成，不可省略**
- ✅ **自检验证：完成后必须执行自检（硬门禁，必须全部通过）**

---

## 执行完成后自检清单

- [ ] 识别出的 N 个模块都生成了模块文档（每文档 12 章完整）？
- [ ] 模块文档间依赖声明一致？
- [ ] 根文档全部 21/23 章？（第12章含聚合展示）
- [ ] 根文档聚合内容与模块文档一致？
- [ ] `docs/logs/` 目录存在，所有核心模块生成了日志文档？
- [ ] 聚合图表交叉验证通过？
- [ ] Mermaid图表语法正确？（无 `[( )]` 圆柱体，ER图实体名无连字符）
- [ ] 所有图表中涉及代码的节点都标注了代码位置？
- [ ] 所有关键代码步骤都在文档中展示了代码片段？
- [ ] 每个核心功能/能力都有独立的时序图？
- [ ] 每个时序图满足详细度要求？（≥6步、≥4参与者、完整分层、有返回消息）
- [ ] 自检全部 6 项通过？（硬门禁）
- [ ] 如有阻塞项，已通过 CP7 委托处理并回归自检通过？
