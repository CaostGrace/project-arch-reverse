---
name: "project-arch-reverse"
description: "逆向分析项目架构。自动检测项目类型（Android/iOS/Java后端/Vue/React/HarmonyOS Next等），分析模块依赖关系，生成含模块依赖图、数据库设计、页面导航的架构文档。**流程图标注方法名+代码文件:行号**。**强制生成所有模块文档，不可省略**。支持增量更新、人工更正和自检验证（最多3次循环）。Trigger phrases: 中文"分析项目结构" "生成架构文档" "项目架构" "逆向分析" "生成项目文档" "模块架构文档" "架构逆向" "项目分析" "自检架构文档" "检查架构完整性" | English "analyze project structure" "generate architecture docs" "reverse engineer project" "project architecture analysis" "module dependency analysis"
---

# 项目架构逆向工程

> TL;DR - 快速执行：检测项目类型 → 扫描模块 → 生成文档 → **自检验证（最多3次循环）** → 确认结果

## 5种常见场景速查 ★

| 场景 | 用户怎么说 | 自动执行策略 | 预计耗时 |
|------|-----------|-------------|----------|
| 首次全量分析 | "帮我分析这个项目" | [A] 全量生成根文档+所有模块文档+核心日志 | 3-10分钟 |
| 更新已有文档 | "项目代码有变化，更新文档" | [C] 增量对比更新变化模块 | 1-5分钟 |
| 新增模块 | "新增了 X 模块，补上文档" | [B] 补充生成缺失的模块文档 | 1-2分钟 |
| 自检验证 | "帮我检查文档完整性" | 检查点2.5 循环自检（最多3次） | 1-3分钟 |
| 只看依赖 | "帮我分析模块依赖" | 跳过完整文档，只生成依赖矩阵+循环检测 | <1分钟 |

## 概述

本 skill 对项目架构进行全面的逆向工程，帮助新团队成员快速理解项目结构，使 AI Agent 能够高效分析代码库。

**核心能力**：自动检测项目类型 | 提取模块依赖 | 检测循环依赖 | **流程图标注代码位置（方法名+文件:行号）** | 分析页面导航 | **强制生成所有模块文档** | **核心日志提取（含增量更新）** | 增量更新 | **自检验证** | 人工更正

## 输入输出规范

### 输入要求

**必要输入**：
- 工作目录（项目根路径）

**可选输入**：
- 指定分析的模块范围（如 `只分析 app 和 feature:* 模块`）
- 指定项目类型（如果不指定则自动检测）

**用户回复格式示例**：
```json
// 全量分析
{"确认": "全部分析"}

// 指定范围
{"范围": ["app", "feature:foryou", "feature:topic"]}

// 跳过某些模块
{"排除": ["build-logic", "gradle-plugin"]}
```

### 输出结构

**生成的文件结构**：
```
{项目根目录}/
└── docs/
    ├── architecture/                     # 架构相关文档
    │   ├── {项目名称}项目架构文档.md      # 根文档（21章）
    │   └── modules/
    │       ├── {模块A}/
    │       │   └── {模块A名称}模块架构.md  # 模块文档（12章）
    │       ├── {模块B}/
    │       │   └── {模块B名称}模块架构.md  # 模块文档（12章）
    │       └── ...                        # 所有模块文档
    └── logs/                              # 日志相关文档
        ├── {模块A}/
        │   └── {模块A名称}_核心日志.md     # 核心日志文档（仅核心模块，5章）
        ├── {模块B}/
        │   └── {模块B名称}_核心日志.md     # 核心日志文档（仅核心模块，5章）
        └── ...                            # 仅核心模块有日志文档
```

### 输出文件命名规范

| 文件类型 | 命名格式 | 示例 |
|----------|----------|------|
| 根文档 | `{项目名}项目架构文档.md` | `MyApp项目架构文档.md` |
| Android模块文档 | `{模块名}模块架构.md` | `app模块架构.md` |
| iOS模块文档 | `{Target名}模块架构.md` | `HomeModule模块架构.md` |
| Web模块文档 | `{目录名}模块架构.md` | `store模块架构.md` |
| 核心日志文档 | `{模块名}_核心日志.md` | `app_核心日志.md` |

### 依赖矩阵输出格式（内联版）

```markdown
| 模块 | 依赖模块 | 依赖类型 |
|------|----------|----------|
| app | feature:foryou | implementation |
| app | feature:topic | implementation |
| feature:foryou | core:data | implementation |
```

详见完整格式：[references/doc-format.md](references/doc-format.md#依赖矩阵格式)

## 检查点流程

> ⚠️ **必须按顺序执行**：
> ```
> 检查点 0（增量检测） → 检查点 1（类型确认）
>     → 【阶段: 项目扫描】 → 【阶段: 文档生成】
>     → 检查点 2（结果确认） → 检查点 2.5（自检验证，最多3次） → 检查点 3（人工更正）
> ```
>
> 详细规范请参阅 `references/` 目录下的对应文件

### 检查点 0：增量检测（必须）

**输入**：工作目录 | **输出**：执行策略 + 检测报告
> → 输出策略后等待用户确认，确认后进入检查点 1

```
1. 扫描 docs/architecture/*.项目架构文档.md → 识别根文档
2. 扫描 docs/architecture/modules/**/*模块架构.md → 识别模块文档
3. 扫描 docs/logs/**/*_核心日志.md → 识别核心日志文档
4. 扫描项目构建文件，识别所有模块
5. 判定核心模块（用户可达界面 + 后台运行能力 + 模块复杂度 + 用户手动指定）
6. 比对生成策略（A/B/C/D）：
   [A] 全量生成：docs/architecture/ 不存在
   [B] 补充生成：根文档存在，模块文档部分缺失
   [C] 增量更新：根文档 + 所有模块文档均存在
   [D] 重建根文档：根文档不存在，模块文档存在
7. 日志文档策略（独立于架构文档）：
   [L-A] 全量生成：docs/logs/ 下核心日志文档均不存在
   [L-B] 补充生成：部分核心模块缺少日志文档
   [L-C] 增量更新：所有核心模块日志文档存在 → 对比代码时间戳更新
8. ⚠️ **输出检测报告并等待用户确认**（不可跳过）：
   展示：当前模块清单、已存在文档清单、推荐策略、核心模块列表
   用户可调整：模块范围（增删）、核心模块标记、策略选择
   用户确认后方可进入检查点 1
```

详见：[references/quick-ref.md](references/quick-ref.md#执行策略选择)

### 检查点 1：项目类型确认（必须）

**输出**：确认的项目类型 + 模块范围
> → 完成后进入【阶段: 项目扫描】

```
1. 文件签名检测（详见 references/project-types-config.md）
2. 加载配置：references/project-types-config.md
3. 确认范围：全部模块 / 指定模块 → 用户确认
```

详见：[references/project-types-config.md](references/project-types-config.md#项目类型检测配置)

### 【阶段: 项目扫描】

> 检查点 1 完成后执行 → 扫描完成后进入【阶段: 文档生成】

```
1. 扫描目录（深度≤5）
2. 解析构建文件 → 提取依赖 → 输出依赖矩阵表格
3. 循环依赖检测
4. 导航分析（nav*.xml / router / App.tsx 等）
5. 生成图表（Mermaid）- ⚠️ 每个步骤标注方法名+代码文件:行号，详见 references/mermaid-spec.md
6. 加载模板：references/{项目类型}-template.md
```

**扫描命令速查**（各语言快速定位入口文件）：
```
# Android: 识别所有模块
find . -maxdepth 3 -name "build.gradle*" | head -20

# iOS: 识别所有 Target
find . -maxdepth 3 \( -name "*.xcodeproj" -o -name "*.xcworkspace" -o -name "Podfile" \)

# Vue/React: 识别源码目录
find . -maxdepth 2 \( -name "package.json" -o -name "src" \) | head -5

# Java (Maven/Gradle): 识别模块
find . -maxdepth 2 \( -name "pom.xml" -o -name "build.gradle*" \)

# Flutter: 识别入口
find . -maxdepth 3 -name "pubspec.yaml"

# HarmonyOS: 识别模块
find . -maxdepth 3 \( -name "hvigorfile.ts" -o -name "oh-package.json5" \)
```

详见：
- [references/quick-ref.md](references/quick-ref.md#模块依赖提取命令) - 依赖提取命令
- [references/doc-format.md](references/doc-format.md#依赖矩阵格式) - 依赖矩阵格式

### 【阶段: 文档生成】

> 项目扫描完成后执行 → 生成完成后进入检查点 2
>
> ⚠️ **所有模块的架构文档都必须生成，不可省略！** 核心模块的日志文档也**必须生成**，不可省略！

**执行策略**：

```
【A 全量生成】  mkdir docs/architecture/ → 生成根文档（21章）→ mkdir docs/architecture/modules/{模块}/ × N → ⚠️ 生成所有模块文档（12章）→ mkdir docs/logs/ → ⚠️ 生成所有核心模块日志文档（5章）
【B 补充生成】  读取现有根文档 → 识别缺失的模块 → ⚠️ 生成所有缺失的模块文档 → 更新根文档 → ⚠️ 补充缺失的核心日志文档
【C 增量更新】  对比代码 vs 文档时间戳 → 只更新变化的模块 → ⚠️ 新模块需生成文档 → ⚠️ 对比更新核心日志文档
【D 重建根文档】基于已有模块文档重建根文档 → ⚠️ 检查并补充缺失模块文档 → ⚠️ 检查并补充缺失核心日志文档
```

**章节速查**（完整定义见 [references/chapters-ref.md](references/chapters-ref.md)）：

| 文档类型 | 章数 | 关键章节（★标注为必须含代码位置） |
|----------|------|----------------------------------|
| 根文档 | 21章 | ★第12章业务流程(含方法+文件:行号)、第6章依赖图、第13章数据库ER图、第20章模块索引 |
| 模块文档 | 12章 | ★第4章业务流程、★第5章数据流、★第6章核心功能流程图、★第11章关键类和方法 |
| 核心日志 | 5章 | ★第3章核心功能日志清单、★第5章常见问题排查指南 |

> 💡 **流程图/时序图标注要求**：每个节点标注 `方法名` + `代码文件:行号`，附带步骤API详解表。详见 [references/mermaid-spec.md](references/mermaid-spec.md)。

**核心日志文档生成规则**：

> ⚠️ **核心日志文档只为核心模块生成**，非核心模块不需要日志文档。

**核心模块判定速查**（满足**任一**即为核心）：

| 判定维度 | 判定条件 | 典型示例 |
|----------|----------|----------|
| 用户界面可达 | 包含用户通过UI到达的页面/界面 | app模块、feature:*模块 |
| 后台运行能力 | 含Service/Worker/定时任务/推送/同步 | core:sync、notification模块 |
| 模块复杂度高 | >20文件或>5000行，含多层抽象 | 核心业务层模块 |
| 用户手动指定 | 用户明确告知（接收"X是核心模块"等表述） | 任意用户指定模块 |

> 💡 **非核心（排除）**：构建脚本、Gradle插件、纯工具库（无UI无后台且复杂度低）、测试模块——但复杂度高或用户指定时可升格。

**核心功能判定速查**（核心模块中满足**任一**即为核心功能）：用户直接交互（按钮/表单/导航）\| 后台自主运行（定时同步/推送/预加载）\| 功能复杂度高（复杂计算/多层调用链/自定义协议）

详见：[references/doc-format.md](references/doc-format.md) | [references/log-extraction-guide.md](references/log-extraction-guide.md)

### 检查点 2：结果确认（必须）

```
1. 展示文档（根文档 + 模块文档索引）
2. 收集反馈：用户检查内容
3. 判定结果：
   - 满足需求 → 继续检查点 2.5
   - 需要修改 → 执行修改 → 重新确认
   - 需要人工更正 → 跳过检查点 2.5 → 进入检查点 3
```

详见：[references/self-check-spec.md](references/self-check-spec.md#检查点2结果确认流程)

### 检查点 2.5：自检验证（必须）

> ⚠️ **此步骤必须执行，不可跳过！**

**自检验证循环机制（最多3次）**：
```
自检 → 发现问题 → 立即处理 → 重新自检
                          ↓ (最多3次)
                    记录残留问题 → 继续下一步
```

**自检流程（5个步骤 + 修复操作）**：

| 步骤 | 检查项 | 发现问题时修复操作 |
|------|--------|-------------------|
| **1. 根架构文档完整性** | 21章逐章检查是否缺失/空/占位符 | 立即调用 `chapters-ref.md` 补充缺失章节；空内容则重新扫描填充 |
| **2. 模块依赖关系正确性** | 第6章依赖图 vs 第7章文本一致性；对每个模块验证其实际依赖 | 修正不一致的依赖描述；重新生成 Mermaid 图；标注需要人工确认的依赖 |
| **3. 模块文档生成状态** | 统计：构建文件识别的模块数 vs 已生成文档的模块数 | 对缺失的模块：分析原因（无源码/空目录/权限），可生成则立即生成；不可生成则记录原因 |
| **4. 核心日志文档完整性** | 核心模块的日志文档是否全部生成；日志是否覆盖所有核心功能 | 缺失日志文档则调用 `log-extraction-guide.md` 提取生成；日志覆盖不足则补充扫描调用链 |
| **5. 汇总并决策** | 汇总以上4步结果 | 输出自检报告（问题清单 + 处理状态）；判定：无问题→继续 或 有处理→重新自检 或 达3次→记录残留 |

> **循环终止条件**：无问题 / 用户确认跳过 / 达到3次循环
> 达到3次后仍有问题 → 记录为残留问题，进入检查点 3（人工更正）时移交处理

详见：[references/self-check-spec.md](references/self-check-spec.md)

### 检查点 3：人工更正（可选）

```
1. 展示文档
2. 收集反馈：[章节X] 的 [内容] → [新内容]
3. 执行更正
4. 重复直到满意
```

## 异常处理

| 场景 | 处理 |
|------|------|
| 文件不可读 | 跳过 + 警告 |
| 构建解析失败 | 目录结构推断 |
| 模块过多 (>50) | 分批（≤20/批） |
| 表过多 (>100) | 只主要表（≥3引用） |
| 非标准结构 | 输出已扫描 + 询问 |
| 增量失败 | 备份 + 全量生成 |
| 导航缺失 | 跳过 + 警告 |
| 循环依赖 | 输出矩阵 + 建议 |
| 模块无源码 | 记录 + 报告 + 询问用户 |
| 模块文档生成失败 | 记录 + 继续下一个 + 最终报告 |
| 核心日志提取失败 | 记录 + 标记为待处理 + 继续下一个模块 |
| 日志语句不足 (<3) | 生成文档但标注"日志覆盖不完整" |

## 参考文档

> 💡 **首次使用建议从 [references/INDEX.md](references/INDEX.md) 开始**，它提供了按用途和检查点的索引。

| 文件 | 用途 |
|------|------|
| [references/INDEX.md](references/INDEX.md) | **入口索引**：按用途/检查点/学习路径分类 |
| [references/quick-ref.md](references/quick-ref.md) | 快速参考（执行预估、场景、策略、命令） |
| [references/chapters-ref.md](references/chapters-ref.md) | 章节速查表（21章+12章+5章详情） |
| [references/mermaid-spec.md](references/mermaid-spec.md) | Mermaid 图表规范（含示例代码） |
| [references/doc-format.md](references/doc-format.md) | 文档格式规范（表格、代码块格式、依赖矩阵） |
| [references/self-check-spec.md](references/self-check-spec.md) | 自检验证详细规格（含确认流程） |
| [references/project-types-config.md](references/project-types-config.md) | 类型检测 + 特定章节配置 + 文件签名 |
| [references/template.md](references/template.md) | 通用模板 |
| [references/*-template.md](references/) | 各项目类型详细模板（8个） |
| [references/build-guide-*.md](references/) | 各项目类型编译指南（8个） |
| [references/log-extraction-guide.md](references/log-extraction-guide.md) | 各语言日志提取指南 |

## 资源使用原则

- ✅ 高效使用：按需加载，不预加载所有资源
- ✅ 路径安全：所有文件操作都经过安全校验
- ✅ 错误恢复：失败时自动降级到备选方案
- ✅ 用户确认：关键操作前必须获得用户确认
- ✅ **强制生成：所有模块文档必须生成，不可省略**
- ✅ **自检验证：完成后必须执行自检流程（最多3次循环）**

## 快速自检清单（执行完成后对照）

在向用户展示结果前，对照此清单快速验证：

- [ ] 根文档是否生成了全部21章？（第12章必须含方法名+文件:行号标注）
- [ ] 项目识别出的 N 个模块都生成了对应的模块文档？
- [ ] 模块文档第4/5/6/11章是否含代码位置标注？
- [ ] 所有核心模块都生成了核心日志文档（5章）？
- [ ] Mermaid 图表语法正确？（无 `[( )]` 圆柱体形状，ER图实体名无连字符）
- [ ] 依赖矩阵中每个模块的依赖类型正确标注？
- [ ] 自检循环是否已执行（至少1次，最多3次）？
- [ ] 如有残留问题，是否已记录到最终报告？
